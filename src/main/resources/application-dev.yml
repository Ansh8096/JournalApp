spring:
  application:
    name: "Journal Application"
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    password: ${REDIS_PASSWORD}
    ssl: true

  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: journaldb
      auto-index-creation: true

  # Because of this properties 'JavaMAilSender' object will get initialized...
  mail:
    host: smtp.gmail.com
    port: 587 # In case when we used encryption...
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true # 'true' means data exchange will be encrypted...

# Kafka Properties:--
  kafka:
    bootstrap-servers: ${KAFKA_SERVER}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: weekly-mood-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: net.engineerAnsh.journalApp.model

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 25MB
server:
  port: 8080

springdoc:
  swagger-ui:
    path: /docs
    syntaxHighlight:
      theme: Monokai

weather:
  API:
    Key: ${WEATHER_API}

app:
  cors:
    allowed-origin: ${ALLOWED_ORIGIN}
  images:
    max-count: 5
    max-size: 5 * 1024 * 1024 # 5MB

cloudinary:
  cloud-name: ${CLOUDINARY_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}
  profile-image-folder: ${CLOUDINARY_PROFILE_IMAGE_FOLDER}
  journal-image-folder: ${CLOUDINARY_JOURNAL_IMAGE_FOLDER}

